const menu=document.querySelector('.menu');const nav=document.querySelector('#global-nav');const header=document.querySelector('.site-header');menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')==='true';menu.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open);header?.classList.remove('header-hidden')});nav?.addEventListener('click',e=>{if(e.target.closest('a')){menu.setAttribute('aria-expanded','false');nav.classList.remove('open')}});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));document.querySelectorAll('details').forEach(d=>{d.addEventListener('toggle',()=>{if(d.open)document.querySelectorAll('details[open]').forEach(x=>{if(x!==d)x.open=false})})});const backToTop=document.querySelector('.back-to-top');let lastScrollY=window.scrollY;const updateOnScroll=()=>{const currentY=window.scrollY;backToTop?.classList.toggle('is-visible',currentY>500);if(menu?.getAttribute('aria-expanded')!=='true'){if(currentY>lastScrollY&&currentY>120)header?.classList.add('header-hidden');else if(currentY<lastScrollY)header?.classList.remove('header-hidden')}if(currentY<=20)header?.classList.remove('header-hidden');lastScrollY=currentY};window.addEventListener('scroll',updateOnScroll,{passive:true});updateOnScroll();document.querySelector('.contact-form')?.addEventListener('submit',e=>{if(e.currentTarget.action.endsWith('#')){e.preventDefault();alert('デモフォームのため送信されません。公開時に送信先を設定してください。')}});
